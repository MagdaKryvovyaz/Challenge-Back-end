<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Facebook</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="../public/style.css">
</head>
<%- include('./partials/nav.ejs')  %> 
<body>
    <div class="container">
        <h3>Add a Post</h3>
        <form style="margin-top: 50px" action="/feed" method="post"> 
            <% if(err) { %>
                <h1 style = "color:red"><%= err.message %></h1>
                  <%}%>
 
                  <div class="mb-3">
                    <label class="form-label">Name</label>
                    <input name ="name" type="text" class="form-control" >
                  </div>
                 
                  <div class="mb-3">
                    <label class="form-label">Text</label>
                    <input name ="message" type="text" class="form-control" >
                  </div>
                  <button type="submit" class="btn btn-outline-success">Submit</button> 
        </form>
    </div>

    <div style="margin-top:20px" class="container">
        <% result.forEach(element => {%>
        <div >
            <div class="card border-primary mb-3"style="width: 24rem;">
                <div id="data-mode" class ="post-info">
                    <div class ="card-header d-flex  justify-content-between">
                        <h5 > <%= element.name %></h5>
                        <p><%= element.created_at%></p>
                    </div>
                    
                    <div class="card-body">
                        <p class="card-text"><%= element.message.slice(0, 20)%></p>
                        <a href="/feed/<%= element.id %>">See more</a>
                       
                    </div>
                </div>
            </div> 
        </div>      
        <% }) %>
  
    </div>
</body>